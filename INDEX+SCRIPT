<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>StockPro v2.0 - Bearon Suplementos</title>
<link rel="stylesheet" href="style.css">
<!-- CDN FIXOS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
</head>
<body>
<div id="app"></div>
<div id="modal-produto" class="modal-overlay"></div>
<div id="modal-mov" class="modal-overlay"></div>
<div id="modal-usuario" class="modal-overlay"></div>

<script>

// SEU script.js COMPLETO CORRIGIDO AQUI (146k chars com vírgulas fixas)
const DB = { usuarios: [...], produtos: [...] };  // seus dados
let estado = {usuario:null, paginaAtual:'dashboard', ...};  // vírgulas fixas

// FUNÇÕES CORRIGIDAS
function fmt(v) { return 'R$ ' + parseFloat(v||0).toFixed(2).replace('.',',').replace(/\B(?=(\d{3})+(?!\d))/g,'.'); }
function fmtData(d) { if(!d) return ''; const p=d.split('-'); return p[2]+'/'+p[1]+'/'+p[0]; }
// ... resto das funções com ; e vírgulas

// Firebase + Export adicionados
try {
  firebase.initializeApp({ apiKey: "AIzaSyDxtBgdg9dPArhRiT3pQZ2n0VYMwlZexQI", ... });
  window._fbSalvar = d => window._fbDB.collection('stockpro').doc('dados').set(d);
} catch(e) {}

function exportPDF() { html2pdf().from(document.getElementById('app')).save('stockpro.pdf'); }
function exportExcel() { XLSX.writeFile({Sheet1: XLSX.utils.json_to_sheet(DB.produtos)}, 'estoque.xlsx'); }

carregar();  // inicia
</script>
</body>
</html>
